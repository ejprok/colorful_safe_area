name: example
description: An example Flutter project for ColorfulSafeArea package.

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  colorful_safe_area:
    path: ../

flutter:
  uses-material-design: true
